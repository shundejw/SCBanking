# PaddleOCR sidecar dependencies.
# numpy is capped below 2.0: paddlepaddle 2.6.2 ships C extensions built
# against the numpy 1.x ABI and breaks (ImportError) on numpy 2.x.
paddlepaddle==2.6.2
paddleocr==2.8.1
fastapi==0.139.0
uvicorn==0.51.0
numpy<2.0
Pillow>=10,<11
setuptools>=70
wheel
