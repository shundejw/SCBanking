# Test profile: only the deltas over the main application.yml.
spring:
  ai:
    openai:
      # Lets the OpenAI autoconfig construct at context load; never called (LLM is mocked).
      api-key: "test-key-not-a-real-secret"
      base-url: "https://api.deepseek.com"
      chat:
        options:
          model: deepseek-v4-flash
          temperature: 0.0

lcchecker:
  artifact:
    root-dir: "target/test-artifacts"   # keep test artifacts out of docs/
